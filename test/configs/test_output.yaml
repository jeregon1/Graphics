# Render Configuration
# Generated automatically

algorithm: photon_mapping
mode: parallel
acceleration: none

# Parallel rendering settings
region_type: column
region_size: 16
num_threads: 8
queue_type: std_queue

# Photon mapping settings
k_photons: 50
radius: 0.1
